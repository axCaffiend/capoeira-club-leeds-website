$(document).ready(function(){
  $('.testimonials').slick({
    infinite: true,
    arrows: true,
    autoplay: true,
    autoplaySpeed: 3000,
  });

  $('.image-gallery--slider').slick({
    infinite: true,
    arrows: true,
    autoplay: true,
    autoplaySpeed: 3000,
  });

  // alert("slick-slider running")
});